import type {Meta, StoryObj} from "@storybook/react"
import Replies from "./Replies.tsx"
 
const meta: Meta <typeof Replies> = {
title: "",
component: Replies,
parameters: {
 
}
}
export default meta
 
type Story = StoryObj <typeof Replies>
 
export const Default: Story = {
 
}
